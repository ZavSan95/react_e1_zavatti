import styled from "styled-components";

const Card = styled.div`

    background: white;
    padding: 2rem;
    margin: 10rem;
    border-radius: .8rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    width: 40rem;

`;

export default Card;